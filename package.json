{
  "name": "pivotcore-products",
  "version": "1.0.0",
  "private": true,
  "description": "PivotCore 製品（型番）ページ静的生成システム",
  "scripts": {
    "build": "node build.js",
    "preview": "node build.js && npx --yes serve dist -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
